<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Quartile (https://www.quartile.co)
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="hr_equipment_request_view_form" model="ir.ui.view">
        <field name="name">maintenance.request.form</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_form" />
        <field name="arch" type="xml">
            <field name="close_date" position="after">
                <field name="maintenance_result" invisible="not done" />
                <field name="followup_request_ids" invisible="1" />
            </field>
            <button name="archive_equipment_request" position="after">
                <button
                    name="action_create_followup_request"
                    type="object"
                    string="Create Follow-up Request"
                    invisible="maintenance_result != 'failed' or followup_request_ids"
                />
            </button>
        </field>
    </record>

    <record id="hr_equipment_request_view_tree" model="ir.ui.view">
        <field name="name">maintenance.request.list</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_tree" />
        <field name="arch" type="xml">
            <field name="stage_id" position="after">
                <field
                    name="maintenance_result"
                    widget="badge"
                    decoration-success="maintenance_result == 'passed'"
                    decoration-danger="maintenance_result == 'failed'"
                    optional="show"
                />
            </field>
        </field>
    </record>

    <record id="hr_equipment_request_view_search" model="ir.ui.view">
        <field name="name">maintenance.request.search</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_search" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='done'][1]" position="after">
                <separator />
                <filter
                    string="Passed"
                    name="passed"
                    domain="[('maintenance_result', '=', 'passed')]"
                />
                <filter
                    string="Failed"
                    name="failed"
                    domain="[('maintenance_result', '=', 'failed')]"
                />
            </xpath>
            <filter name="stages" position="after">
                <filter
                    string="Maintenance Result"
                    name="maintenance_result"
                    context="{'group_by': 'maintenance_result'}"
                />
            </filter>
        </field>
    </record>
</odoo>
